<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Редактировать транспорт</title>
    <link rel="stylesheet" href="/css/style.css">
</head>
<body>
    <header>
        <h1>Редактировать транспортное средство</h1>
        <a href="/" class="btn">Назад к списку</a>
    </header>

    <main>
        <form id="transportForm">
            <input type="hidden" id="id" name="id" value="<%= transport.id %>">

            <div class="form-group">
                <label for="type">Тип транспорта:</label>
                <select id="type" name="type" required>
                    <option value="Грузовик" <%= transport.type === 'Грузовик' ? 'selected' : '' %>>Грузовик</option>
                    <option value="Фура" <%= transport.type === 'Фура' ? 'selected' : '' %>>Фура</option>
                    <option value="Микроавтобус" <%= transport.type === 'Микроавтобус' ? 'selected' : '' %>>Микроавтобус</option>
                    <option value="Рефрижератор" <%= transport.type === 'Рефрижератор' ? 'selected' : '' %>>Рефрижератор</option>
                </select>
            </div>

            <div class="form-group">
                <label for="brand">Марка:</label>
                <input type="text" id="brand" name="brand" value="<%= transport.brand %>" required>
            </div>

            <div class="form-group">
                <label for="licensePlate">Гос. номер:</label>
                <input type="text" id="licensePlate" name="licensePlate" value="<%= transport.licensePlate %>" required>
            </div>

            <div class="form-group">
                <label for="capacity">Грузоподъемность (кг):</label>
                <input type="number" id="capacity" name="capacity" value="<%= transport.capacity %>" required>
            </div>

            <div class="form-group">
                <label for="status">Статус:</label>
                <select id="status" name="status">
                    <option value="Доступен" <%= transport.status === 'Доступен' ? 'selected' : '' %>>Доступен</option>
                    <option value="В ремонте" <%= transport.status === 'В ремонте' ? 'selected' : '' %>>В ремонте</option>
                    <option value="В рейсе" <%= transport.status === 'В рейсе' ? 'selected' : '' %>>В рейсе</option>
                </select>
            </div>

            <button type="submit" class="btn">Сохранить</button>
        </form>
    </main>

    <script src="/js/app.js"></script>
</body>
</html>