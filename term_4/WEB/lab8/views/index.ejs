<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Транспортная компания</title>
    <link rel="stylesheet" href="/css/style.css">
</head>
<body>
    <header>
        <h1>Транспортная компания</h1>
        <a href="/create" class="btn">Добавить транспорт</a>
    </header>

    <main>
        <h2>Список транспортных средств</h2>
        
        <% if (transports.length === 0) { %>
            <p>Нет доступных транспортных средств</p>
        <% } else { %>
            <table>
                <thead>
                    <tr>
                        <th>Тип</th>
                        <th>Марка</th>
                        <th>Гос. номер</th>
                        <th>Грузоподъемность</th>
                        <th>Действия</th>
                    </tr>
                </thead>
                <tbody>
                    <% transports.forEach(transport => { %>
                        <tr>
                            <td><%= transport.type %></td>
                            <td><%= transport.brand %></td>
                            <td><%= transport.licensePlate %></td>
                            <td><%= transport.capacity %> кг</td>
                            <td class="actions">
                                <a href="/<%= transport.id %>" class="btn btn-sm">Просмотр</a>
                                <a href="/<%= transport.id %>/edit" class="btn btn-sm">Редактировать</a>
                                <button class="btn btn-sm btn-danger" onclick="deleteTransport('<%= transport.id %>')">Удалить</button>
                            </td>
                        </tr>
                    <% }); %>
                </tbody>
            </table>
        <% } %>
    </main>

    <script src="/js/app.js"></script>
</body>
</html>